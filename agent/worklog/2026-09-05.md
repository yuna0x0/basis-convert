# 2026-09-05

Synced the Basis clone to current upstream (17 commits) and checked the package against it.

## What upstream changed that is near us

- `JigglePhysics`, `JiggleTree`, `JiggleMemoryBus`: allocation and profiler-marker work inside
  the simulation. `JiggleRigData` and the serialized fields the writer sets are untouched.
- `BasisConstraintSystem`: the solver's dependency sort moved to flat arrays. The constraint
  components themselves did not change.
- The avatar allow-list lost `BasisJiggleBonesComponent` and `BasisMediaPlayerNetworked` and
  gained `BasisPermissionEventShim`. Nothing we write is affected; `JiggleRig` and every Vixxy
  type are still listed. `DestroyEditorOnlyInAvatar` still runs before a build.
- `JIGGLE_VALIDATE` left the scripting defines. Nothing in the package references it.
- The blendshape stripper call in `BasisAvatarSDKInspector` is still commented out, so the
  `BasisBlendshapeBuildHooks` item in the 2026-09-02 Later list has not started.
- `BasisAvatar`, `AvatarSDKVisemes`, `BasisAuthoredMotion`, `BasisMotionClip` and the HVR Vixxy
  types were not touched.

236 tests pass against the synced project, no `error CS`. No package change needed.

## Colliders, checked against the sources

Asked whether VRM spring bone colliders reach the jiggle rig. They do: a probe over both
AvatarSample_B files matched every spring's collider count to the plan and all 212 planned
entries to the source radius and offset. VRChat's do too; the Shinano scene holds 105 rigs
carrying them. Checking the semantics against the decompiled SDK and the jiggle job found two
things the tests could not:

- **Capsule height.** VRChat and Dynamic Bone give it end to end and treat a capsule no taller
  than its diameter as a sphere; jiggle measures between the cap centres. Every converted capsule
  was a diameter too long. Fixed in both mappers; the VRM one was already right, since VRM states
  the two centres.
- **Plane orientation.** VRChat rotates a plane, Dynamic Bone picks its axis, VRM states its
  normal. A jiggle plane faces its transform's Y and has no field for anything else, so a plane
  facing another way was written facing Y without a word. Now reported on all three sources.
  Nothing else can be done short of writing a helper transform, which is not started.

Details in `agent/research/physbone-to-jiggle-mapping.md`. Eight new tests in
`ColliderShapeTests`.

## Next

Unchanged from 2026-09-04: the four images, the two jiggle heuristics, Magica Cloth 2. A
helper transform for turned plane colliders is a candidate if the diagnostic comes up in use.
